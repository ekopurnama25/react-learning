// import Cookies from "js-cookie";

// export const setCookie = (data) => {
//   let setencodedCookiesToken = JSON.stringify(data);
//   Cookies.set("users", setencodedCookiesToken);
// };

// export const UpdatesetCookie = (accessToken) => {
//   let setencodedCookiesToken = JSON.stringify(accessToken);
//   Cookies.set("users", setencodedCookiesToken);
// };

// export const getCookie = () => {
//   let users = Cookies.get("users");
//   if (typeof users !== "undefined") {
//     // console.log(JSON.parse(users));
//     return JSON.parse(Cookies.get("users"));
//   }
//   return null;
// };
